# Figures

- `fig_h0_recovery_ladder.{pdf,png}` — normalised H0 posterior per completeness rung (complete / m<20 / m<19 / m<18) overlaid, one colour per rung, truth line, medians ticked at the baseline. Generated by `scripts/fig_recovery_ladder.py` from `results/anch_*.{h5,json}` and `results/summary.json`.
