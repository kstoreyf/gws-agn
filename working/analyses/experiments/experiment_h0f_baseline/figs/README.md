# Figures

- `fig_h0_recovery.{pdf,png}` — H0 marginal posterior per planted-f rung (joint grid marginalised over f), one colour per rung, truth line, medians ticked; only f = 0.307 and 0.703 carry joint (H0, f) scans — the f = 0.0099 and 1.0 rungs exist solely as f-scans at fixed true H0 and contain no H0 information. Generated by `scripts/fig_recovery_h0.py` from `results/jointzoom_fagn{0.3,0.7}.h5` (fallback `joint_fagn*`).
- fig_h0_recovery_allrungs.{pdf,png}: H0 posterior at the PLANTED f for all four rungs (h0scan_fagn*, incl. the 0.0/1.0 pair from run_h0scan_extra.sh); offsets {-3.51, -1.04, -3.50, +0.33} — non-monotonic in f, see TILT_FINDINGS.md for the two-lever explanation.
