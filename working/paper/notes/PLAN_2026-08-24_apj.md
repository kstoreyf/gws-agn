# PLAN 2026-08-24 (evening): retarget ApJ, full-story section rewrite

Orchestrated execution (Fable orchestrator, Opus workers, owner-requested
workflow). Repo clean at `0f7f6fb`. Owner directives of this session:

1. Journal target is **ApJ** (no main-text word limit, no figure cap;
   abstract 250 hard). The class stays `aastex631`; the journal is chosen at
   the portal.
2. **Every experiment enters the story**, section by section, coherently.
   Owner Q&A this session: analysis 7 stays **internal** (PARTIAL, residual
   unexplained); analysis 0 (pure tracer) is **promoted from the appendix
   into Results**; the appendix goes away.
3. **Introduction is rewritten** (owner: "needs work").
4. Style bound by `physics-first-results` + `astro-paper-style`; "an astro
   paper, not a lab journal".

## Context

- Manuscript at 0f7f6fb: 12 pp, compile clean, audit 201/201, all plotted
  intervals 90%, prose 68%. Sections: intro, methods, data, results
  (single / joint / incomplete), validation, discussion, appendix (pure
  tracer).
- Experiment coverage before this plan: a0 appendix; a1 §4.1+controls;
  a2 §4.2+validation; a3 ladder, a4 density sensitivity, a5 free anchors +
  three-realisation adjudication all in §4.3. Missing: **a6's
  relative-completeness null**. Excluded by standing owner decisions
  (2026-08-12/24, upheld today): the retracted relative-completeness law,
  faintward bias, oracle probe, a7 occupancy axis, AGN=complete cells.

## Headline decision

Unchanged from PLAN_2026-08-24_incomplete.md (gated there on on-disk JSONs).
The a6 addition is a null with pre-computed numbers in
`analysis_6_relative_completeness_H0_fagn/results/surface_summary.json` and
per-cell JSONs; no new production runs.

## Workstreams

- **A1 (Opus)**: three new macros over the SIX both-flux-limited cells of
  analysis 6 (`\HzeroRelSlope`, `\HzeroRelSpan`, `\FagnRelSpan`), appended to
  build_values.py, mirrored in audit_values.py, documented in NUMBERS.md with
  the derivation and the one-cell-error caveat; existing macros byte-identical;
  audit exit 0.
- **B-intro, B-methods, B-data, B-results, B-validation, B-discussion
  (Opus, parallel)**: per-section rewrite/polish per the contracts in
  STORY_2026-08-24_apj.md. B-results absorbs the appendix into a new
  subsection and adds the a6 null sentence group.
- **C (Opus, fresh)**: integration (main.tex appendix removal, README journal
  line), build_values, compile x3, gate report (errors/overfull/undefined,
  pdftotext greps, abstract count with linenumbers off).
- **Orchestrator pass** between C and reviews: abstract, seams, independent
  recomputation of the three new macros.
- **D1 (Opus, fresh)**: claims-vs-numbers adversarial review vs JSONs.
- **D2 (Opus, fresh)**: style review per astro-paper-style self-check.

## Execution order

WF1 = [A1 + B x 6 in parallel] -> C. Orchestrator pass. WF2 = [D1 + D2].
Orchestrator adjudicates, fixes, final gates, commits per workstream.

## Key files

Modified: sections/*.tex (all six), main.tex, references.bib (3 verified
entries added by orchestrator: Abbott2017SirenH0, Planck2018, Riess2022),
scripts/build_values.py, scripts/audit_values.py, values/results_macros.tex,
NUMBERS.md, README.md. Deleted: sections/appendix_pure_tracer.tex (content
absorbed into results.tex). New: notes/STORY_2026-08-24_apj.md, this file.
Figures: unchanged (no new figure; the a6 null earns a sentence, not a
figure, per the internal-tests rule).

## Verification

audit_values exit 0; compile 0 errors / 0 overfull / 0 undefined; abstract
<=250 rendered with linenumbers off; banned-word + em-dash + author-note
greps on the rendered PDF = 0; agreement claims checked against NUMBERS.md
containment flags; orchestrator independently recomputes \HzeroRelSlope,
\HzeroRelSpan, \FagnRelSpan from the per-cell JSONs; all D1/D2 findings
fixed or recorded; owner-only items restated at close.

## Status log

- WF1 complete (8/8): A1 macros verified against an independent orchestrator
  recomputation (\FagnRelSpan 0.016, \HzeroRelSpan 0.45, \HzeroRelSlope
  -0.074; audit 204/204); six sections rewritten; integrator gates all clean
  except the abstract (253 rendered).
- Orchestrator seam pass done: gap claim re-scoped with three verified new
  references (Bartos2017NatComm, Veronesi2022, Veronesi2023); intro
  "catalogs contain" ambiguity fixed; results hand-typed "five" ->
  \ClosureNseeds; "within the horizon" -> "within the volume the events
  occupy"; relative-depth null given its own paragraph; data "recovers" ->
  "returns" on the bias ratio; methods 2.3 retitled "Completion of
  flux-limited catalogs" + \citep{Speagle2020} on nested sampling; abstract
  rewritten to 249 merged tokens (255 raw pdftotext) with the closure claim
  in mean-offset form; stale .ipynb_checkpoints removed. Clean rebuild:
  0 errors / 0 overfull / 0 undefined, 13 pp.
- WF2 (D1 claims-vs-numbers + D2 style) launched, pending.

## Risks

- Parallel section workers drift apart in voice -> STORY contracts fix each
  section's opening claim and hand-off; orchestrator seam pass before review.
- surface_summary.json may not precompute the six-cell H0 stats -> A1
  derives from `joint_g*_a*_s100.json` medians and records the derivation.
- Intro literature numbers -> workers may cite ONLY existing bib keys and
  may quote only the literature values listed in the STORY contract;
  anything else goes to a MISSING-REF note for the orchestrator.
- The abstract is at the 250 cap -> orchestrator owns it; any addition pays
  with a cut, counted from the render.
